services:
  - type: web
    name: code-breaker-game
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: code-breaker-db
          property: connectionString
      - key: NODE_ENV
        value: production